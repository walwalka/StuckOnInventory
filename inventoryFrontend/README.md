# coinListsFrontend

All documentation has moved to the root README:

- [../README.md](../README.md)

Notes:
- Uses a relative `/api` base via Nginx to reach the backend.
- Served at http://localhost:8080 when running with Docker Compose.
