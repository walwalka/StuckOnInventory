# coinListsBackend

All documentation has moved to the root README:

- [../README.md](../README.md)

Notes:
- API served under `/api` (e.g., `/api/login`, `/api/coins`, `/api/mintlocations`).
- On startup, backend reconciles `mintlocations` from `database/mints.json`.
